name: Build and deploy

# Built here rather than by GitHub Pages' classic Jekyll build, which pins
# Jekyll 3.9 and an allowlist of plugins. This runs the same Jekyll 4 as local.
#
# One-time setup: repository Settings -> Pages -> Source must be "GitHub Actions",
# or the deploy job fails on its first run.

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Let a running deploy finish; queue at most one more.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"
          bundler-cache: true

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      # check_sentences.py reads the collection's YAML front matter; check_links.py
      # is stdlib only.
      - name: Install Python dependencies
        run: pip install pyyaml

      # Runs before the build: _kanji/ is hand-maintained source, and a sentence
      # tagged with the wrong word is worth catching before 2,284 pages are
      # rendered from it.
      - name: Check the example sentences
        run: python scripts/check_sentences.py

      - name: Configure Pages
        id: pages
        uses: actions/configure-pages@v5

      - name: Build
        # baseurl/url come from _config.yml; configure-pages sets them too, and
        # the two must agree or every asset 404s.
        run: bundle exec jekyll build --trace
        env:
          JEKYLL_ENV: production

      - name: Verify every internal link resolves
        run: python scripts/check_links.py

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
